exports.sendThyme = (data, client) => {
    // var toThyme = new net.Socket();
    // toThyme.connect(3105, 'localhost', () => toThyme.write(JSON.stringify(data)));

    // send to thyme
    // var _client = net.connect(3105, '192.168.0.19', () => {
    //     _client.write(JSON.stringify(data));
    //     console.info('<----- data sent ---- '+ JSON.stringify(data));
    // });

    
}

